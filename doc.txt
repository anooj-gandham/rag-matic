1. using openai embeddings directly in weaviate after chunking the document
2. used django as it offers very robust framework for the developement flexibility.
3. Used celery + rabbitmq for long running jobs like generating embeddings.
